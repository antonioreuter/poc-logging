"use strict";

enum LogCategory {
    TRACE_LOG = "TraceLog",
    EVENT_LOG = "EventLog",
    ISSUE_LOG = "IssueLog",
    FATAL_LOG = "FatalLog",
    REQUEST_LOG = "RequestLog"
}

export { LogCategory };